#include "FatalErrorHandler.h"

//#include <windows.h>
//#include "SDL.h"

void FatalError(const wchar_t * message)
{
  //MessageBox(0, message, L"NateCommander Fatal Error", 0);
  //SDL_Quit();
  //exit(-1);
}

void FatalError(const wchar_t * message, const char * extraInfo)
{
  //MessageBox(0, message, L"NateCommander Fatal Error", 0);
  //MessageBoxA(0, extraInfo, "NateCommander Fatal Error", 0);
  //SDL_Quit();
  //exit(-1);
}